import React, { useState } from 'react';

const EmployeeChange = ({isOpen, onClose, selectedUser}) => {

    const [userInfo, setUserInfo] = useState({

    })
    return (
        <div>
            
        </div>
    );
};

export default EmployeeChange;